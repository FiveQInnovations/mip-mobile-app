# Reusable step: Verify that the app header renders correctly
# Note: appId is required by Maestro even for subflows (though it's inherited at runtime)
appId: com.fiveq.ffci
---
- assertVisible:
    id: "search-outline"
